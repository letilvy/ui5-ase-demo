<mvc:View
	controllerName="sap.ui.demo.bulletinboard.controller.Worklist"
	xmlns="sap.m"
	xmlns:mvc="sap.ui.core.mvc"
	xmlns:semantic="sap.m.semantic">
	<semantic:FullscreenPage
		id="page"
		title="{i18n>worklistViewTitle}">
		<semantic:content>
			<Table
				id="table"
				width="auto"
				class="sapUiResponsiveMargin"
				growing="true"
				items="{
					path: 'posts>/',
					sorter: {
						path: 'Title',
						descending: false
						}
					}"
				busyIndicatorDelay="{worklistView>/tableBusyDelay}"
				updateFinished="onUpdateFinished">
				<headerToolbar>
					<Toolbar>
						<Label id="tableHeader" text="{worklistView>/worklistTableTitle}"/>
						<ToolbarSpacer />
						<SearchField id="searchField" width="auto" search="onFilterPosts"/>
					</Toolbar>
				</headerToolbar>
				<columns>
					<Column width="40%" id="nameColumn" vAlign="Middle">
						<Text text="{i18n>TableNameColumnTitle}" id="nameColumnTitle"/>
					</Column>
					<Column width="30%" id="categoryColumn" vAlign="Middle">
						<Text text="{i18n>TableCategoryColumnTitle}" id="categoryColumnTitle"/>
					</Column>
					<Column width="15%" id="unitNumberColumn" hAlign="End" vAlign="Middle">
						<Text text="{i18n>TableUnitNumberColumnTitle}" id="unitNumberColumnTitle"/>
					</Column>
					<Column width="10%" id="flaggedColumn" demandPopin="true" vAlign="Middle"/>
				</columns>
				<items>
					<ColumnListItem
						vAlign="Middle"
						type="Navigation"
						press="onPress">
						<cells>
							<ObjectIdentifier
								title="{posts>Title}"/>
							<Text
								text="{posts>Category}"/>
							<ObjectNumber
								number="{
										path: 'posts>Price',
										formatter: '.formatter.numberUnit'
									}"
								state="{
										path: 'posts>Price',
										formatter: '.formatter.priceState'
									}"
								unit="{posts>Currency}"/>
							<ToggleButton
								id="flaggedButton"
								tooltip="{i18n>flaggedTooltip}"
								icon="sap-icon://flag"
								pressed="{
										path: 'posts>Flagged',
										type: '.types.flagged'
									}"
								class="sapUiMediumMarginBeginEnd"/>
						</cells>
					</ColumnListItem>
				</items>
			</Table>
		</semantic:content>
		<semantic:sendEmailAction>
			<semantic:SendEmailAction id="shareEmail" press="onShareEmailPress"/>
		</semantic:sendEmailAction>
	</semantic:FullscreenPage>
</mvc:View>
