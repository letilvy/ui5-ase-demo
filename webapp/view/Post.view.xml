<core:View 
	xmlns:core="sap.ui.core" 
	xmlns:mvc="sap.ui.core.mvc" 
	xmlns="sap.m"
	xmlns:form="sap.ui.layout.form"
	controllerName="sap.ui.demo.bulletinboard.controller.Post" 
	xmlns:semantic="sap.m.semantic">

	<semantic:FullscreenPage
			id="page"
			busy="{postView>/busy}"
			busyIndicatorDelay="0"
			showNavButton="true"
			navButtonPress="onNavBack"
			title="{i18n>objectTitle}">
	    <semantic:favoriteAction>
	        <semantic.FavoriteAction
	            pressed="{Favorite}"
	            press="onPressFavoriteBtn" />
	    </semantic:favoriteAction>
		<semantic:content>
			<ObjectHeader
				id="objectHeader"
				title="{Title}"
				responsive="true"
				number="{Price}"
				numberUnit="{Currency}"
				showMarkers="true"
				markFavorite="{Favorite}">
			    <!-- 
				// Bug of UI5 that visibility of ObjectMarker does not take effect when data binding change.
				// So we switch back to use deprecated attribute 'markFavorite'
				// Should use 'ObjectMarker' after UI5 fix the bug
			    <markers>
			        <ObjectMarker
			            visible="{Favorite}"
			            visibility="IconOnly"
			            type="Favorite" />
			    </markers> -->
			</ObjectHeader>
			<IconTabBar id="iconTabBar"
				expanded="{device>/isNoPhone}"
				class="sapUiResponsiveContentPadding">
				<items>
					<IconTabFilter icon="sap-icon://hint" key="info">
						<form:SimpleForm>
							<form:content>
								<Label text="{i18n>postDateLabel}"/>
                        		<Text text="{
                        			path: 'Timestamp',
                        			formatter: '.formatter.date'	
                        		}"/>
                        		<Label text="{i18n>postDescriptionLabel}"/>
                       		 	<Text text="{Description}"/>
							</form:content>
						</form:SimpleForm>
					</IconTabFilter>
					<IconTabFilter icon="sap-icon://inspection" key="statistics">
						<Text text="Viewed 55555 times" id="viewCounter"/>
					</IconTabFilter>
				</items>
			</IconTabBar>
		</semantic:content>
	</semantic:FullscreenPage>
</core:View>