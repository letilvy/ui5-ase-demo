# Exercise: Controller Testing
1. Add count of itmes to the title, e.g. Posts (23)

			/* Task: 
					Add count of itmes to the title, e.g. Posts (23)
					If no item in the worklist, then display 'Posts'  
				Hint:
					oEvent already provided a parameter as count of items
			*/
